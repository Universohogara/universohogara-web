version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Iniciando despliegue de sitio estatico."
  artifacts:
    # Esto le dice a AWS que los archivos estan en la carpeta principal
    baseDirectory: / 
    files:
      - '**/*'
