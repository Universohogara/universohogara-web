version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Iniciando despliegue de sitio estático."
  artifacts:
    # La ruta de inicio (Root)
    baseDirectory: / 
    files:
      - '**/*'
  cache:
    paths: []
