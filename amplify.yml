version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Iniciando despliegue de sitio estático."
  artifacts:
    # Esto le dice a AWS que los archivos están en la carpeta principal
    baseDirectory: / 
    files:
      - '**/*'
