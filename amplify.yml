version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Buscando archivos en la carpeta principal."
  artifacts:
    # ¡ESTO ES LO QUE CAMBIA! - Buscamos en una subcarpeta común
    baseDirectory: public 
    files:
      - '**/*'
