import React from 'react';
import { Metadata } from 'next'; // Importamos el tipo Metadata

// 1. Agregamos metadatos para forzar al compilador a procesar este layout
export const metadata: Metadata = {
  title: 'Universo Hogara - Premium',
  description: 'Contenido exclusivo para suscriptores.',
};

// 2. El componente se mantiene simple
export default function PremiumLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <section className="bg-white min-h-screen">
      {children}
    </section>
  );
}
